fastapi>=0.115.0
uvicorn[standard]>=0.34.0
python-multipart>=0.0.18
python-dotenv>=1.0.0
pydantic>=2.10.0
pydantic-settings>=2.7.0
boto3>=1.35.0

# Computer vision — Roboflow runtime + tracking/annotation.
# `inference` pulls opencv/onnxruntime/numpy and is a HEAVY install. The
# default `rfdetr-base` COCO model runs locally with NO API key; weights
# auto-download on first use. ffmpeg/ffprobe are system binaries (see README),
# not pip packages.
inference>=0.30.0
supervision>=0.25.0

# AI moment summaries (optional at runtime — pipeline degrades gracefully).
anthropic>=0.40.0

# Dev / quality
ruff>=0.8.0
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.27.0
